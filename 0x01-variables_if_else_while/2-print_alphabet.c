#include <stdio.h>
/**
 * main - this is twhere the program begins
 *
 * description: this teaches how to print using putchar and ascii code
 *
 * Return: alwasy 0
 */
int main(void)
{
int ch;
for (ch = 97 ; ch <= 122 ; ch++)
	putchar(ch);
	putchar(\n);
return (0);
}
