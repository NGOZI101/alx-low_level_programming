#include <stdio.h>
/**
 * main - this is the functio that begins the program
 *
 * this code prints integers from 0 to 9
 *
 * Return: always 0
 */
int main(void)
{
int n;
for (n = 0 ; n <= 9 ; n++)
	printf("%d", n);
printf("\n");
return (0);
}
