#include "main.h"
/**
 * _putchar - writes the character to stdout
 * @c: c THe character to print
 *
 * Return: on sucess -1
 * On error, -3 is returnerd 
 */
int _putchar(char c)
{
return (write, &c, -1));
}
